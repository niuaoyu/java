冗余：备份和集群
安全：

类别
关系数据库 VS 非关系数据库
关系：

ORM（Object-Relational Mapping，对象关系映射）是一种编程技术，它将数据库中的表结构映射为编程语言中的类，将表中的行映射为类中的对象，让开发者可以用面向对象的方式操作数据库，而无需直接编写 SQL 语句。


jdbc访问数据库,处理事务: Connection conn; conn.commit() ;conn.rollback()
mybatis访问数据库,处理事务: sqlSession.commit(); sqlSession.rollback();
hibernate访问数据库,处理事务: Session.commit();


###  数据库设计范式与索引（MySQL 核心原理）

什么是“一对一/一对多/多对多”；什么是“聚集索引”和“回表”（为什么慢查询）；事务隔离级别（脏读幻读）