
所谓的 **Agent Loop（智能体循环）**，本质上是 ReAct 模式

模型决策 → 宿主执行 → 结果回填 → 模型再决策，循环往复

直到模型觉得任务完成、只输出普通文本为止


**模型永远只做一件事：根据上下文生成下一段文本（可能是结构化的工具调用请求）**。

所有"操控终端""读文件""连外部服务"的动作，都是 Claude Code 这个宿主程序在模型"请求"之后，用普通的系统编程手段（子进程、文件 I/O、网络请求/JSON-RPC）真实执行的，再把结果文本喂回模型。

模型和外部世界之间没有直接连接，中间这层执行引擎才是"手"，模型只是"大脑发指令"