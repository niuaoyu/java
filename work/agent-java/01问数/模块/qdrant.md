

qdrant适用于中小型项目，企业级万亿数据量建议选择Milvus

操作，导包，创建服务器之后，然后把自己的一些自定义的关键字以及对应的信息（每一个行包含id，向量，payload，这个payload就是要找的信息），导入qdrant服务器

id：点的唯一标识符，可以是UUID或字符串。
vector：向量的值，是一个列表。
payload：附加信息，是一个字典，键值对形式。

既然是模糊查询，那返回的值也必须有所限制，不能相关就返回，









