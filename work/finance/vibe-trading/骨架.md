
prompt：

你现在的任务是给我做一次代码考古(code archaeology),对象是 Vibe-Trading 这个
仓库(HKUDS/Vibe-Trading)。请先浏览仓库目录结构和关键源文件,然后按下面的
结构给我讲清楚它的"框架骨架"是怎么搭的。

【严格排除】
不要解释任何具体的交易/量化业务逻辑,包括但不限于:
- 具体的回测算法、alpha因子公式、技术指标计算
- 具体的broker连接器实现细节(下单参数、鉴权流程细节)
- 具体的数据源解析逻辑(某个loader怎么解析某个API返回值)
遇到这些直接跳过或一句话带过,把篇幅留给框架本身。

【请重点解释以下内容,并且每一点都要指出对应在仓库里的哪个目录/文件】

1. 顶层模块划分
   - 列出顶层目录(agent/ frontend/ scripts/ tools/ 等)各自的职责边界
   - 哪些目录是"核心运行时"、哪些是"接入层"、哪些是"周边工具"
   - 模块之间的依赖方向是怎样的(谁依赖谁,有没有循环依赖被刻意规避)

2. Session 的数据结构与生命周期
   - Session 对象/文件包含哪些字段(id、时间戳、消息历史、meta等)
   - 一个 session 从创建到结束经历哪些状态,状态之间怎么流转
   - session 的持久化格式是什么(JSON/JSONL/数据库),写入时机是什么
     (每轮追加 vs 定期落盘),为什么选这种格式
   - 崩溃恢复怎么做的(比如中途进程挂了,历史会不会丢、怎么补救)
   - resume 一个历史 session 的完整代码路径:从CLI输入命令开始,
     到实际把历史消息重新载入agent loop,中间经过了哪些函数/类

3. Session 与多个接入端的关系
   - CLI / REST API / MCP / Web UI / IM渠道(Telegram等)是否共享
     同一个 session runtime,共享的边界在哪(哪一层是公共的,
     哪一层是每个接入端各自适配的)
   - 新增一个新的接入端(假设我要接一个新的IM渠道),理论上需要
     实现哪几个接口/继承哪个基类,不需要碰哪些代码

4. Agent 核心循环与工具调用机制
   - loop 的整体结构是什么样的(接收输入→模型推理→工具调用→
     结果回填→继续,大概几步)
   - 工具是怎么注册进这个loop的,loop本身是否感知工具的具体实现
   - 多个provider(不同LLM厂商)是怎么被统一适配进同一个loop的,
     这层抽象在哪个文件

5. 上下文压缩与记忆管理
   - 一个session的历史多长时候会触发压缩,触发条件写在哪
   - 压缩策略是什么样的分层(哪些内容优先被丢、哪些被保留)
   - 长期记忆(persistent memory)和session内的短期历史是什么关系,
     是两套独立机制还是有联动

6. 并发与流式
   - SSE/流式输出是怎么接入session的,一个session能否被多个客户端
     同时订阅(比如网页开着,同时CLI也在看)
   - 如果多个worker(比如swarm多agent模式)同时运行,session状态
     是怎么隔离或者共享的,有没有加锁/文件锁之类的机制

7. 用一张简化的架构图(文字描述即可,不需要真的画图)串联上面这些点,
   说明一条完整请求的生命周期:从用户在CLI里敲一行字开始,到agent
   给出最终回复、session落盘,整个过程经过的模块顺序。

最后请用一句话总结:如果我要照着这个项目的思路,从零搭一个类似的
session框架,最核心、最不能偷懒的设计决策是哪一个,为什么。