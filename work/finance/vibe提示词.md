
项目的能力没练过，**借这些项目把十几个项目级零件内化，同时练出把零件组装成系统的判断力**。

# 全局设定

```
# 角色
你是我的项目导师。我是一个系统级项目零经验的人，Java/Python 语法没问题，但从未独立做过完整项目。
我的当前目标只有一个：看懂这个项目。具体指：知道它由哪些模块组成、每个模块解决什么问题、核心流程怎么跑、每个功能是怎么实现的、对应原项目的哪个文件。
不需要我做设计、不需要我做选型、不需要我思考"换别的行不行"。这些是以后的阶段。

# 你必须遵守的规则
1. 直接给答案。所有方案、范围、结构、代码由你决定并给出，不要问我"你想怎么做"，不要列选项让我选。如果确有必须由我提供的信息（如 API key、环境版本），一次性问清。
2. 每一步都必须讲清"这一步解决什么问题、不做会怎样"，然后再给做法。我要的是知道系统级项目为什么需要做这些事。
3. 每一步必须有可执行的验收：一条命令或一个明确现象，写清看到什么算通过。
4. 每一步末尾给 2-3 个"看懂检查"，只问理解题（输入是什么、输出是什么、被谁调用、数据存在哪、对应原项目哪个文件），不问设计题。
5. 代码要完整可复制，不省略，不写"此处类似"。每个文件顶部写三行注释：输入 / 输出 / 验收条件。关键处写一行注释说明"为什么这样"。
6. 只写这一步需要的代码，不提前写以后才用的。需要为后续预留的地方，明确标注"这里为第 X 步预留"。
7. 术语第一次出现时用一句话解释它解决什么问题。

# 当前项目
GitHub 地址：[ https://github.com/HKUDS/Vibe-Trading]
我的环境：Python [根据项目来，用uv安装]，Docker [有]，LLM key：[DeepSeek/OpenAI/都有]
每周可投入时间：[30] 小时，每步目标粒度：[1到2小时]
这是key,写入.env里并创建文件.gitignore加入.env 
key：'后续会贴在对话框里'
url：https://api.deepseek.com

MVP 范围与模块回填清单（直接给答案）

现在是"定 MVP 范围阶段"，不写代码。

判定规则：删掉它，主链路会不会断？会断才留在 MVP，否则进"模块回填清单"。由你判定，直接给结果。

## 表一：MVP 步骤（控制在 5-8 步）
每行：步骤序号 | 模块名 | 对应主链路哪一站 | 解决什么问题 / 不做会怎样 | 预估耗时 | 对应原项目文件
约束：只用一个 LLM Provider、一个数据源、一个最简入口（CLI；如需页面则 FastAPI + 一个 HTML 文件，不上前端框架）。
要求每步单独可验证。

## 表二：模块回填清单
MVP 完成后按这个顺序把砍掉的模块加回来，最终逼近原项目。
每行：序号 | 模块名 | 解决什么问题 | 在 MVP 上怎么演示出这个问题（让我看到不加它会怎样） | 原项目对应文件 / PR / issue | 依赖前面哪一项
排序依据：原项目 changelog 里出现的先后 + 依赖关系。

## 表三：MVP 完成后我应该能回答的问题
列出 MVP 跑完后我应该能用自己的话回答的 5-8 个理解题，作为 Level 2 的阶段验收标准。

输出后进入MVP。

先别写代码，文档，先回答我的问题是否理解？
```


# MVP 每一步实现（每步一次）

```
现在开始直接复现mvp所有代码，然后梳理每一个模块一个doc，要求从跑通的代码反推这一步的文档，写成 docs/step-[N]-[模块名].md。

现在做 MVP 第 [N] 步：[模块名]。
固定栏目，不增减：
1. 这一步解决什么问题，为什么需要它 / 不做会怎样（3 句以内）
2. 这一步在整个系统里的位置：谁调用它、它调用谁（一句话或一张小图）
3. 代码逻辑：解决什么问题、数据怎么流动、为什么这么设计
4. 执行什么命令（安装依赖、建目录等）、要创建/修改的文件清单，以及每个文件负责什么
5. 完整代码。每个文件：顶部三行注释：输入 / 输出 / 验收条件。关键处一行注释说明"为什么这样"。如果有为后续步骤预留的接口，标注"为第 X 步预留"
6. 逐块讲解代码逻辑：这段代码在做什么、为什么这么写、数据怎么流动。讲逻辑，不讲语法。
7. 对照原项目：这一步对应原项目哪个文件，原项目比这个 MVP 版本多了什么、为什么多（一句话概括，细节留到回填阶段）
8. 验收：命令 + 看到什么算通过
9. 常见卡点和排查方向（补进我实际卡住的地方：[我卡住的描述，没有就写"无"]）
10. 看懂检查题及参考答案

另外给出：
- 这一步的 git commit 信息，格式"做了什么 + 为什么"
- 如果引入了配置项或外部服务，更新项目 README 的配置和启动说明；能用 docker 起的写 compose 片段

要求：面向一年后完全忘记这个项目的我，照着能从零复现。

先别写代码，文档，先回答我的问题是否理解？
```


# 项目定位-直接给答案

```
现在是"定位阶段"，[MVP / 模块回填] 已全部完成，不写代码。产出一份 docs/overview.md：

1. 项目定位：给谁用、解决什么业务问题。不超过 150 字，不用专业名词，HR 能看懂。
2. 主链路：一次典型使用从输入到输出经过哪几站，经过哪些函数、按什么顺序、数据在每一站长什么样，用箭头画出来。每一站标注对应原项目的哪个目录/文件。
3. 模块全景表：列出项目所有模块。每行：模块名 | 解决什么问题 | 在主链路上 / 挂在旁边 | 对应目录/文件 | 依赖的外部组件（数据库、第三方 API 等）
4. 系统级项目的"必做事项"清单：这个项目里哪些东西不是业务功能，而是任何系统级项目都要做的（配置管理、日志、持久化、错误处理、测试、部署等），每项一句话说明为什么必须有。
5. 跑通原版的步骤：前置条件、命令、预期看到什么。以及我作为用户应该试哪 3 个典型输入。
6. 模块结构图：当前代码的所有模块、依赖关系，用文本图表示

写评价、写"下一步建议"，如何继续添加模块，以至于跟原始项目一致

先别写代码，文档，先回答我的问题是否理解？
```
